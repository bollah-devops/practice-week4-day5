replicaCount: 3

namespace: staging

env:
  ENV_NAME: staging
  VERSION: "1.1.0"

secret:
  SECRET_KEY: staging-secret-key-123

service:
  type: LoadBalancer
  port: 80
  targetPort: 5000
  nodePort: 30090

ingress:
  path: /staging
